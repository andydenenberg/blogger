<h1>Posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Updated at</th>
    <th>Created by</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th>Comments</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.updated_at.strftime ("%Y-%m-%d %H:%M:%S") %></td>
    <td><%= post.created_by %></td>


    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, confirm: 'Are you sure?', method: :delete %></td>
    <td><%= link_to 'New', new_comment_path %></td>
   <td><%= link_to post.comments.count %></td>
 </tr>
<% end %>
</table>

<br />


<%= link_to 'New Post', new_post_path %>

<p>
<%= "Current Logged in User: #{session[:name]}" %>

<p>
<%= link_to 'Home', home_index_path %>
